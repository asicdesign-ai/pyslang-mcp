Use `pyslang-mcp` if available to analyze lowRISC Ibex's `ibex_counter.sv`.

Summarize the module parameters, ports, generated counter structure, and reset
behavior. Include diagnostics status and avoid simulation or synthesis claims.
