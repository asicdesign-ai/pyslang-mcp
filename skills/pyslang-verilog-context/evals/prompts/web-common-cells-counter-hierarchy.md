Use `pyslang-mcp` if available to analyze PULP common_cells `counter.sv` with
`delta_counter.sv`.

Report the design units and hierarchy under `counter`, including the
`delta_counter` instance. Include diagnostics status.
