Use `pyslang-mcp` if available to analyze the APB timer project from
`project.f`.

Find where `irq_o` is declared and referenced, then explain how it relates to
the wrapper and timer core. Include any limitations of the symbol evidence.
