Use `pyslang-mcp` if available to analyze `simple_counter.sv`.

Summarize the module interface, state, reset behavior, and main control
priority. Include the compiler diagnostics status and avoid claims that require
simulation or synthesis.
