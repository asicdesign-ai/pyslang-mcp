Use `pyslang-mcp` if available to analyze the `project.f` filelist for the
`sync_fifo` project.

Report the design units, package/include context, and hierarchy below
`sync_fifo`. Explain what the compiler evidence can and cannot prove about the
FIFO.
