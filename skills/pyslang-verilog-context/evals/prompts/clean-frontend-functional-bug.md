Use `pyslang-mcp` if available to analyze the APB timer bug project from
`project.f`.

Check frontend diagnostics and hierarchy first. Then inspect the IRQ clear and
terminal-count behavior and explain any likely functional issue. Be explicit
that clean diagnostics are not a functional proof.
