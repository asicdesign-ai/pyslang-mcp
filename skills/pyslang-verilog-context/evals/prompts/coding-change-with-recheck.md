Use `pyslang-mcp` if available to inspect `edge_detect.sv` before editing.

Add or propose a scoped RTL change that introduces a registered one-cycle
`any_edge_seen` output while preserving the existing edge pulse behavior. Re-run
or plan a frontend diagnostics check after the change.
